<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App1.Views.AgendamentosUsuarioView"
             Title="Meus Agendamentos">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="listViewAgendamentos" ItemsSource="{Binding Lista}" Margin="25">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding DataFormatada}" FontSize="20" VerticalOptions="Center"/>
                                    <Label Text=" - " VerticalOptions="Center"/>
                                    <Label Text="{Binding Modelo}" FontAttributes="Bold" VerticalOptions="Center"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>